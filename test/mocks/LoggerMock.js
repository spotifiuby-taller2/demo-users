const setLevel = (level) => {
}

const info = (message) => {
}

const debug = (message) => {
}

const warn = (message) => {
}

const error = (message) => {
}

const request = (message) => {
}

module.exports = {
    setLevel,
    info,
    debug,
    warn,
    error,
    request
}
